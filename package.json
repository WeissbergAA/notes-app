{
  "name": "notes-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,consumer,web -c blue,magenta,green \"npm run dev:api\" \"npm run dev:consumer\" \"npm run dev:web\"",
    "dev:api": "npm run dev -w @notes/api",
    "dev:web": "npm run dev -w @notes/web",
    "dev:consumer": "npm run dev -w @notes/consumer",
    "setup": "bash scripts/setup.sh",
    "sync-env": "bash scripts/sync-env-from-infra.sh",
    "status": "bash scripts/status.sh",
    "free-ports": "bash scripts/free-ports.sh",
    "kill-ports": "bash scripts/free-ports.sh --all",
    "docker:app": "docker compose -f docker-compose.app.yml up --build",
    "lint": "npm run build -w @notes/shared && npm run prisma:generate -w @notes/api && npm run lint -ws --if-present",
    "test": "npm run test -ws --if-present",
    "build": "npm run build -w @notes/shared && npm run prisma:generate -w @notes/api && npm run build -ws --if-present",
    "test:e2e": "npm run test:e2e -w @notes/api"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
